export const DEFAULT_META = {
  table: "",
  primary: ["id"],
};
Object.freeze(DEFAULT_META);

export const DEFAULT_MODEL = {
  id: { field: "id", label: "ID", system: true },
};
Object.freeze(DEFAULT_MODEL);
